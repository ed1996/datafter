<%= content_for :meta_title, "List des hommages Datafter | #{DEFAULT_META["meta_title"]}" %>
<%= content_for :meta_description, "Liste de tout les hommages disponible | #{DEFAULT_META["meta_description"]}" %>
<div class="container">
  <h2 class="divider offset-top-40 offset-md-top-80 text-center">Tous les hommages pour animaux</h2>
  <div class="range">
    <div class="cell-md-8 cell-md-preffix-2">
      <%= render 'layouts/searchanimal' %>
    </div>
  </div>
  <%= render 'layouts/animals' %>
</div>