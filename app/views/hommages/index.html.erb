<div class="row">
  <div class="col-md-3">
    <ul class="sidebar-list">
      <li class="sidebar-item"><%= link_to "Mes hommages", hommages_path, class:"sidebar-link active" %></li>
    </ul>
  </div>
  <div class="col-md-9">
    <div class="panel panel-default">
      <div class="panel-heading"> Mes hommages</div>
      <%= render 'layouts/search' %>
      <div class="panel-body">
        <% @hommages.each do |hommage| %>
        <div class="row">
          <div class="col-md-2">
            <%= image_tag hommage.photos[0].image.url(:thumb) if hommage.photos.length > 0 %>
          </div>
          <div class="col-md-7">
            <h4><%= hommage.first_name %> <%= hommage.last_name %></h4>
          </div>
          <div class="col-md-3 right">
            <%= link_to "Mettre à jour", edit_hommage_path(hommage), class:"btn btn-primary" %>
          </div>
        </div>
            <hr>
            <% end %>
      </div>
    </div>
  </div>
</div>